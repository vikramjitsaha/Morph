# proto-agent dependencies
# Install with: pip install -r requirements.txt

# HTTP client (async + streaming)
httpx>=0.27.0

# Environment variable loading
python-dotenv>=1.0.0

# Terminal dashboard
rich>=13.7.0

# Async support (stdlib in Python 3.11+ but explicit for older envs)
asyncio; python_version < "3.11"

# Optional: faster JSON parsing
orjson>=3.9.0
